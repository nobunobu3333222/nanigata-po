# 何型っぽ？｜UI / UX仕様

## 1. 基本方針

本サービスはスマートフォン利用を最優先とする。

目的は、

```text
SNS / 検索から流入
↓
すぐ診断開始
↓
16問をストレスなく回答
↓
結果を楽しむ
↓
本当の血液型と答え合わせ
↓
SNS / LINEへ共有
```

という流れを最短で成立させること。

デザインは、

**ポップ / 親しみやすい / カジュアル / 少し可愛い / 子どもっぽすぎない**

を基本とする。

占いサイトによくある神秘的・スピリチュアルな雰囲気には寄せない。

---

# 2. デザインコンセプト

## キーワード

```text
Pop
Friendly
Playful
Simple
Mobile-first
Shareable
```

## 目指す印象

- SNSで見かけても怪しく見えない
- 20〜30代でも使いやすい
- 男女どちらでも使いやすい
- 文字が読みやすい
- 診断結果をスクショしたくなる
- 友達に送りやすい

---

# 3. ベースカラー

## 通常画面

背景：

```text
#FFFDF8
```

もしくは類似の淡いオフホワイト。

メイン文字：

```text
#222222
```

サブ文字：

```text
#666666
```

カード：

```text
#FFFFFF
```

境界線：

```text
#EAEAEA
```

---

# 4. 血液型別テーマカラー

結果画面ではprimaryTypeに応じてテーマカラーを変更する。

## A型

イメージ：

```text
Coral / Pink
```

参考：

```text
Primary: #FF7187
Light:   #FFF0F3
```

## B型

イメージ：

```text
Yellow / Orange
```

参考：

```text
Primary: #FFB72B
Light:   #FFF6DA
```

## O型

イメージ：

```text
Sky Blue
```

参考：

```text
Primary: #55B7F3
Light:   #EAF7FF
```

## AB型

イメージ：

```text
Purple
```

参考：

```text
Primary: #9A79E8
Light:   #F4EFFF
```

正確なカラーコードは実装時に微調整可能。

重要なのは、結果画面で血液型ごとの違いが一目で分かること。

---

# 5. タイポグラフィ

日本語フォントは可読性の高いゴシック体を使用する。

Google Fonts等を利用する場合は、日本語表示速度への影響を考慮する。

候補：

```text
Noto Sans JP
Zen Kaku Gothic New
M PLUS Rounded 1c
```

ただし可愛くなりすぎる場合はNoto Sans JPを優先する。

## ウェイト

```text
Hero / Result:
700〜900

Heading:
700

Body:
400〜500

Button:
700
```

数字や血液型文字は大きく太くする。

---

# 6. レイアウト

基本最大幅：

```text
max-width: 560px
```

PCでも診断部分は中央寄せとし、スマートフォン画面に近い体験を維持する。

左右余白：

```text
mobile: 20px
desktop: 24px
```

画面最小高さ：

```text
min-height: 100dvh
```

---

# 7. 角丸

サービス全体で丸みを持たせる。

```text
Cards: 20〜24px
Buttons: 16〜20px
Small badges: 9999px
```

角ばったUIは避ける。

---

# 8. シャドウ

強い影は使用しない。

必要な場合：

```text
0 4px 16px rgba(0,0,0,0.06)
```

程度に抑える。

基本は、

```text
背景色
+
境界線
+
余白
```

でカードを区別する。

---

# 9. 画面構成

MVPでは以下の画面を実装する。

```text
/
トップ

/quiz
診断

/result
診断結果

/about
診断について

/blood-type/a
A型解説

/blood-type/b
B型解説

/blood-type/o
O型解説

/blood-type/ab
AB型解説
```

診断中の質問番号についてURLを個別変更する必要はない。

---

# 10. トップページ

## ファーストビュー

中央寄せ。

表示内容：

```text
何型っぽ？

16問でわかる
血液型印象診断

あなたは周りから
何型っぽく見られそう？

[無料で診断する]

約2分
登録不要
```

### メインCTA

```text
無料で診断する
```

画面内でもっとも目立つボタンにする。

高さ：

```text
56px以上
```

幅：

```text
100%
```

---

## ファーストビュー装飾

背景または周囲に、

```text
A
B
O
AB
?
```

などの文字を小さな丸・カードとして配置してもよい。

ただしキャラクターイラストはMVPでは使用しない。

---

# 11. トップページ下部

以下を簡潔に表示する。

## セクション1

### 何がわかる？

```text
日常の行動から、
あなたがどんな印象に見られそうかを分析。

その結果を
A / B / O / ABの
「血液型っぽさ」で表します。
```

---

## セクション2

### こんな結果が出ます

簡易モックカード：

```text
A型っぽさ 78%

きっちり安心タイプ

ちゃんとしていて、
任せると安心な人
```

---

## セクション3

### たった16問

```text
旅行
LINE
友達
休日
SNS
```

などの日常シーンから回答することを示す。

---

## セクション4

### 本当の血液型と答え合わせ

```text
予想はA型
実際はO型

そんなギャップも楽しめます。
```

---

# 12. 診断開始画面

トップCTA押下後、/quizへ遷移。

最初に短い説明を表示する。

```text
16問の日常シーンから
あなたの「見られ方」を診断します。

正解・不正解はありません。

実際の自分に一番近いものを
直感で選んでください。

[診断スタート]
```

補足：

```text
※エンタメとしてお楽しみください。
```

---

# 13. 質問画面

## 上部

```text
何型っぽ？
```

小型ロゴ。

その下：

```text
Q 03 / 16
```

進捗バー。

---

## 進捗バー

画面横幅いっぱい。

例：

```text
━━━━━━━━━━━━━━
```

完了率に合わせてテーマアクセント色で伸びる。

診断中は血液型別カラーを使用しない。

ニュートラルなアクセントカラーを使用する。

---

# 14. 質問カード

例：

```text
Q3

友達との待ち合わせ。
普段はどのくらいに着く？
```

質問文は大きく表示。

目安：

```text
20〜24px
font-weight: 700
line-height: 1.5
```

---

# 15. 回答ボタン

4択を縦並び。

例：

```text
┌──────────────────┐
  10分以上前に着くことが多い
└──────────────────┘

┌──────────────────┐
  だいたい時間通りに着く
└──────────────────┘
```

仕様：

```text
width: 100%
min-height: 64px
border-radius: 18px
```

タップ領域を十分に広くする。

ホバー可能端末では軽いhover表現を使用する。

---

# 16. 回答時アニメーション

回答ボタン押下時：

```text
scale 1
↓
scale 0.97
↓
selected state
↓
次の質問
```

時間：

```text
150〜250ms
```

程度。

長い演出は禁止。

回答後、自動で次問へ進む。

「次へ」ボタンは置かない。

---

# 17. 戻る操作

画面上部に、

```text
←
```

を配置。

前の質問へ戻れるようにする。

戻って回答し直した場合、以前の回答を上書きする。

Q1の場合は診断開始画面へ戻る。

---

# 18. 診断途中の保存

回答状況はlocalStorage等に保存する。

ページリロード時は、

```text
途中から続ける
最初からやり直す
```

を表示可能。

MVPでは自動復元でもよい。

---

# 19. 診断完了演出

Q16回答後、即結果表示しない。

短い分析演出画面を表示。

例：

```text
あなたの「見られ方」を
分析しています…

計画性
慎重さ
社交性
自由度
```

2秒以内を目安とする。

実際の計算処理が終了していても、最低500〜800ms程度は表示してよい。

過剰な待機は避ける。

---

# 20. 結果画面 Hero

結果画面はprimaryTypeのテーマカラーへ切り替える。

例：A型

```text
あなたは……

A型っぽ！

78%

きっちり安心タイプ

ちゃんとしていて、
任せると安心な人
```

## 視覚優先順位

1.

```text
A型っぽ！
```

2.

```text
78%
```

3.

```text
きっちり安心タイプ
```

4.

短い説明。

---

# 21. 背景演出

結果Heroでは背景に、

```text
A
```

の巨大文字を薄く配置してよい。

例：

```text
font-size: 220px
opacity: 0.05
```

小さな丸・星・波線などの抽象装飾も使用可能。

ただし派手すぎないこと。

---

# 22. 結果カード

Hero以降はカード形式。

## 強みカード

見出し：

```text
あなたのいいところ
```

内容：

```text
✓ 準備が丁寧

✓ 安心感がある
```

---

## 弱点カード

見出し：

```text
ちょっとだけ注意
```

内容：

```text
△ 考えすぎることも
```

ネガティブに見えすぎない表現にする。

---

# 23. 7軸プロフィール

見出し：

```text
あなたの見られ方
```

MVPではレーダーチャートより横棒グラフを使用する。

理由：

- スマホで読みやすい
- 数値比較しやすい
- 実装が軽い
- スクショでも分かりやすい

表示：

```text
計画性      82
████████░░

慎重さ      88
█████████░

社交性      51
█████░░░░░
```

全バーはprimaryTypeカラーを使用。

7種類別カラーにはしない。

---

# 24. 軸の詳細

各軸をタップまたは展開すると、

```text
計画性 82

先のことを考えて準備するタイプ

予定や段取りを決めてから
動く傾向が強く出ています。
```

を表示可能。

MVPでは常時文章表示でもよい。

---

# 25. 「なぜこの結果？」カード

見出し：

```text
なんでA型っぽい？
```

表示：

```text
こんな回答が結果につながりました。

・旅行では予定を決めておきたい
・初めてのお店は口コミを調べる
・ミスしたら原因を振り返る
```

最大3項目。

最後に：

```text
慎重さ・計画性・気配りが
強く出たことが、
A型っぽい印象につながりました。
```

---

# 26. 隠れ血液型

secondaryTypeを小型カードで表示。

例：

```text
隠れAB型っぽさも

AB型っぽさ 67%

一歩引いて考えたり、
自分なりの感覚を持つ部分には
AB型っぽさもあります。
```

primaryTypeほど目立たせない。

---

# 27. 本当の血液型入力

結果内容を十分に見せたあとに配置。

見出し：

```text
ところで、
本当の血液型は？
```

ボタン：

```text
[A型] [B型]
[O型] [AB型]
[わからない]
```

A/B/O/ABは2×2。

「わからない」は横幅100%。

---

# 28. 答え合わせ演出

選択するとカードを展開。

一致例：

```text
🎉

やっぱりA型！

予想：A型
実際：A型
```

不一致例：

```text
おっ、意外！

予想：A型
実際：O型

血液型は違いましたが、
行動からはA型っぽい印象が
強く出ました。
```

「ハズレ」「不正解」は基本使用しない。

---

# 29. シェアエリア

答え合わせの前でも後でも共有可能にする。

メインボタン：

```text
結果をシェアする
```

サブ：

```text
LINEで送る
Xでシェア
リンクをコピー
```

Web Share APIが使用可能な端末では、

```text
navigator.share()
```

を優先してもよい。

---

# 30. シェア画像

SNS用画像を生成。

推奨比率：

```text
1:1
```

または

```text
4:5
```

表示内容：

```text
何型っぽ？

A型っぽ！
78%

きっちり安心タイプ

ちゃんとしていて、
任せると安心な人
```

個人情報やactualTypeは入れない。

---

# 31. もう一度診断

ページ最下部：

```text
もう一度診断する
```

secondary CTAとして配置。

押した場合：

```text
answers削除
scores削除
actualType削除
```

してQ1へ戻る。

---

# 32. フッター

最低限：

```text
何型っぽ？

診断について
プライバシーポリシー
お問い合わせ
```

下部：

```text
この診断は一般的に語られる
血液型イメージをもとにした
エンタメコンテンツです。
```

---

# 33. 血液型解説ページ

SEO用ページ。

各ページ：

```text
/blood-type/a
/blood-type/b
/blood-type/o
/blood-type/ab
```

基本構成：

```text
A型っぽい人の特徴とは？

A型っぽく見られやすい特徴
A型っぽく見える行動
長所として見られやすい部分
誤解されやすい部分

[自分はA型っぽい？診断する]
```

診断CTAをページ上部・中部・下部に配置する。

---

# 34. PC表示

PCでも内容を横に広げすぎない。

診断：

```text
max-width: 560px
```

結果：

```text
max-width: 720px
```

SEO記事：

```text
max-width: 800px
```

中央配置。

---

# 35. アクセシビリティ

最低限対応：

- 本文文字サイズ16px以上
- 十分な色コントラスト
- 色だけで結果を伝えない
- ボタン高さ44px以上
- keyboard操作可能
- focus状態を消さない
- aria-labelを適切に設定
- reduced-motion対応

`prefers-reduced-motion` が有効な場合はアニメーションを大幅に減らす。

---

# 36. ローディング

ページ全体のローディング演出は極力減らす。

質問切り替えはクライアント上で即時。

結果計算もクライアント処理を基本とする。

データ保存が失敗しても結果表示自体は妨げない。

---

# 37. エラー時

回答不足で/resultへ直接アクセスされた場合：

```text
診断結果が見つかりません

もう一度診断してみよう！

[診断をはじめる]
```

Supabase保存失敗時はユーザーに大きなエラーを出さない。

```text
診断結果は表示可能。
保存処理のみ再試行または無視。
```

---

# 38. レスポンシブ基準

主要確認幅：

```text
375px
390px
430px
768px
1440px
```

特に375px幅で、

- 横スクロールしない
- 回答文が窮屈にならない
- ボタンが押しやすい
- 結果画面が崩れない

ことを確認する。

---

# 39. MVPでは実装しないもの

以下はPhase 2以降。

```text
ユーザー登録
マイページ
友達診断
コメント機能
ランキング
AIチャット
友達との比較グラフ
ネイティブアプリ
PWAインストール誘導
通知機能
キャラクター
```

MVPでは診断体験に集中する。

---

# 40. UIコンポーネント候補

```text
Header
Logo
PrimaryButton
SecondaryButton
QuizProgress
QuestionCard
AnswerOption
ResultHero
TraitCard
AxisBar
ReasonCard
SecondaryTypeCard
BloodTypeSelector
MatchResultCard
ShareButtons
Disclaimer
Footer
```

各コンポーネントは再利用可能にする。

---

# 41. 実装時の最優先事項

優先順位：

```text
1. スマホで回答しやすい
2. 結果が一瞬で理解できる
3. 結果画面がスクショ映えする
4. シェアしやすい
5. 読み込みが速い
6. PCでも破綻しない
```

装飾よりも、この6点を優先する。

---

# 42. 全体イメージ

ユーザーに与えたい感覚は、

```text
「ちょっとやってみよう」
↓
「テンポよく答えられる」
↓
「お、A型っぽいんだ」
↓
「理由もなんかわかる」
↓
「実際O型なの面白い」
↓
「友達に送ろう」
```

とする。

診断自体を難しく見せず、

**軽く遊べるが、結果には少し納得感がある**

というバランスを最重要とする。